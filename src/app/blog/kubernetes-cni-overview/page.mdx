---
title: "Understanding Kubernetes CNI: How Pods Get Their Network"
description: "Tong quan ve CNI trong Kubernetes - tu network model, so sanh cach GKE va AWS EKS trien khai networking, den security groups va performance benchmarking"
date: 2026-04-04
---

## CNI la gi?

<div style={{display: 'flex', justifyContent: 'center', alignItems: 'center', gap: '2rem', margin: '2rem 0', flexWrap: 'wrap'}}>
  <img
    src="/images/kubernetes-cni-overview/kubernetes-horizontal-color.png"
    alt="Kubernetes Logo"
    style={{maxWidth: '250px', height: 'auto'}}
  />
  <img
    src="/images/kubernetes-cni-overview/cncf-color.png"
    alt="CNCF Logo"
    style={{maxWidth: '180px', height: 'auto'}}
  />
</div>

**CNI (Container Network Interface)** la mot du an cua Cloud Native Computing Foundation (CNCF) bao gom specification va libraries de cau hinh network interfaces trong Linux containers. CNI chi tap trung vao hai viec:

- **Ket noi network** cho container khi duoc tao
- **Don dep network** khi container bi xoa

CNI khong quan ly DNS, service discovery, hay load balancing - do la viec cua cac thanh phan khac trong Kubernetes.

<div style={{display: 'flex', justifyContent: 'center', margin: '2rem 0'}}>
  <img
    src="/images/kubernetes-cni-overview/cni-diagram.png"
    alt="CNI Architecture - Orchestrator, CNI Library, Plugins"
    style={{maxWidth: '500px', height: 'auto'}}
  />
</div>

## The Kubernetes Network Model

Kubernetes dat ra cac yeu cau co ban cho networking:

1. Moi Pod co mot **IP duy nhat** trong toan cluster
2. Pod tren moi node co the giao tiep voi nhau **KHONG can NAT**
3. Agents tren node (kubelet, system daemons) co the giao tiep voi tat ca Pod tren node do

Nghe don gian, nhung trien khai thuc te rat khac nhau tuy cloud provider. Hay xem GKE va AWS EKS giai quyet bai toan nay nhu the nao.

<div style={{display: 'flex', justifyContent: 'center', margin: '2rem 0'}}>
  <img
    src="/images/kubernetes-cni-overview/eks-network-overview.png"
    alt="Kubernetes Node Network Namespaces - veth pairs connecting Pod namespaces to host"
    style={{maxWidth: '600px', width: '100%', height: 'auto'}}
  />
</div>

## GKE: Routes-Based vs VPC-Native

Google Kubernetes Engine cung cap hai che do networking:

### Routes-Based Mode

<div style={{display: 'flex', justifyContent: 'center', margin: '2rem 0'}}>
  <img
    src="/images/kubernetes-cni-overview/gke-routes-based.webp"
    alt="GKE Routes-Based Mode - Custom static routes giua cac node"
    style={{maxWidth: '700px', width: '100%', height: 'auto'}}
  />
</div>

Dac diem:
- Moi node nhan mot dai `/24` rieng (~254 Pod IPs per node)
- **Pod IPs khong duoc dang ky trong VPC** - chung chi ton tai o muc node
- GKE tu dong tao routes giua cac node dua tren Pod CIDR
- Traffic cross-node di qua node IP roi duoc forward den Pod

**Networking ben trong node**: Pods co network namespace rieng biet. Node ket noi voi Pod qua **veth pair**, tat ca veth interfaces ket noi vao bridge `cbr0` (Layer 2).

<div style={{display: 'flex', justifyContent: 'center', margin: '2rem 0'}}>
  <img
    src="/images/kubernetes-cni-overview/gke-node-networking.webp"
    alt="GKE Node Internal Networking - veth pairs, cbr0 bridge, eth0"
    style={{maxWidth: '500px', width: '100%', height: 'auto'}}
  />
</div>

### VPC-Native Mode (Khuyen nghi)

<div style={{display: 'flex', justifyContent: 'center', margin: '2rem 0'}}>
  <img
    src="/images/kubernetes-cni-overview/gke-vpc-native.webp"
    alt="GKE VPC-Native Mode - Alias IP ranges per node"
    style={{maxWidth: '700px', width: '100%', height: 'auto'}}
  />
</div>

VPC-Native su dung **Alias IP Ranges** - mot network interface co the co nhieu IP ranges. Uu diem so voi Routes-Based:

| | Routes-Based | VPC-Native |
|---|---|---|
| Pod IP routable trong VPC | Khong | **Co** |
| Firewall granularity | Per-node | **Per-pod range** |
| Can custom routes | Co | **Khong** |
| On-premises access | Phuc tap | **Qua VPN/Interconnect** |
| NEG support | Khong | **Co** |

> VPC-Native mode khong co nhuoc diem so voi Routes-Based. Google khuyen nghi luon su dung VPC-Native.

## AWS EKS: VPC CNI

<div style={{display: 'flex', justifyContent: 'center', alignItems: 'center', gap: '2rem', margin: '2rem 0', flexWrap: 'wrap'}}>
  <img
    src="/images/kubernetes-cni-overview/aws-eks-icon.svg"
    alt="AWS EKS"
    style={{maxWidth: '80px', height: 'auto'}}
  />
</div>

AWS EKS su dung **Amazon VPC CNI** - cach tiep can hoan toan khac: **moi Pod nhan mot IP thuc tu VPC subnet**.

### Kien truc

<div style={{display: 'flex', justifyContent: 'center', margin: '2rem 0'}}>
  <img
    src="/images/kubernetes-cni-overview/vpc-cni-architecture.png"
    alt="AWS VPC CNI Architecture - kubelet, VPC CNI, L-IPAM, ENIs"
    style={{maxWidth: '700px', width: '100%', height: 'auto'}}
  />
</div>

### Hai thanh phan chinh

**1. CNI Binary** - chay tren root filesystem cua node:
- Duoc kubelet goi khi Pod duoc tao/xoa
- Cau hinh network cho Pod (veth pair, route, ARP)
- Su dung ky thuat `/32` IP + link-local gateway `169.254.1.1`

**2. ipamd (IP Address Management Daemon)** - chay lien tuc tren moi node:
- Quan ly cac ENI (Elastic Network Interface) tren node
- Duy tri **warm pool** cac IP address san sang gan cho Pod
- Tu dong request them ENI/IP khi pool can

### Pod networking ben trong node

```text
Pod Network Namespace:
+--------------------------------------+
|  eth0: 172.31.23.163/32              |
|                                      |
|  Route table:                        |
|  default via 169.254.1.1 dev eth0    |
|  169.254.1.1 dev eth0 scope link     |
|                                      |
|  ARP:                                |
|  169.254.1.1 -> MAC(veth_host_side)  |
+--------------------------------------+
```

> Nhan ra pattern nay chua? Day chinh xac la ky thuat **Secondary IP voi Link-Local Gateway** ma chung ta da thuc hanh trong bai [Container Networking from Scratch](/blog/container-networking-from-scratch)!

### Gioi han

So Pod tren moi node bi gioi han boi instance type:

```text
Max Pods = (So ENI toi da) x (So IP moi ENI - 1)

Vi du t3.medium:
  3 ENIs x (6 IPs - 1) = 15 Pods

Vi du m5.xlarge:
  4 ENIs x (15 IPs - 1) = 56 Pods
```

## Security Groups cho Pods

### GKE: Firewall Rules

GKE su dung VPC Firewall Rules voi cac thanh phan:

| Thanh phan | Mo ta |
|---|---|
| Network | VPC network |
| Priority | 0-65535 (thap hon = uu tien cao hon) |
| Direction | Ingress / Egress |
| Action | Allow / Deny |
| Target | All instances / Tags / Service accounts |
| Source | IP ranges / Service accounts / Tags |
| Destination | IP ranges (v4/v6) |
| Protocol | TCP / UDP / ICMP / ... |
| Ports | Specific ports hoac all |

Voi **VPC-Native mode**, firewall rules co the target **specific pod IP ranges** thay vi toan bo node - giup security granular hon nhieu.

### AWS EKS: Security Groups for Pods

AWS cho phep gan **security group rieng cho tung Pod** (khong chi cho node). Co che nay su dung **trunk ENI**:

<div style={{display: 'flex', justifyContent: 'center', margin: '2rem 0'}}>
  <img
    src="/images/kubernetes-cni-overview/security-groups-pods.png"
    alt="AWS Security Groups for Pods - Primary ENI, Secondary ENI, Trunk ENI with branch ENIs"
    style={{maxWidth: '700px', width: '100%', height: 'auto'}}
  />
</div>

Kich hoat bang cach set `ENABLE_POD_ENI=true` tren VPC CNI, sau do tao SecurityGroupPolicy CRD:

```yaml
apiVersion: vpcresources.k8s.aws/v1beta1
kind: SecurityGroupPolicy
metadata:
  name: my-security-group-policy
  namespace: my-namespace
spec:
  podSelector:
    matchLabels:
      role: my-role
  securityGroups:
    groupIds:
      - sg-0123456789abcdef0
```

Moi Pod voi label matching se nhan **ENI rieng** voi security group chi dinh, thay vi dung chung ENI voi node.

## So sanh cac CNI pho bien

<div style={{display: 'flex', justifyContent: 'center', alignItems: 'center', gap: '2rem', margin: '2rem 0', flexWrap: 'wrap'}}>
  <img
    src="/images/kubernetes-cni-overview/cilium-icon.png"
    alt="Cilium"
    style={{maxWidth: '60px', height: 'auto'}}
  />
  <img
    src="/images/kubernetes-cni-overview/flannel-logo.png"
    alt="Flannel"
    style={{maxWidth: '150px', height: 'auto'}}
  />
</div>

| | Flannel | Calico | Cilium | AWS VPC CNI |
|---|---|---|---|---|
| **Data plane** | VXLAN / host-gw | iptables / eBPF | eBPF | VPC native |
| **Network Policy** | Khong | Co | Co (L3-L7) | Security Groups |
| **IPAM** | host-local | Calico IPAM | Cilium IPAM | ENI-based |
| **Encryption** | Khong | WireGuard | WireGuard / IPsec | VPC encryption |
| **Observability** | Co ban | Co ban | Hubble (rat manh) | VPC Flow Logs |
| **Complexity** | Thap | Trung binh | Cao | Thap (managed) |
| **Use case** | Dev/test | Production general | Production advanced | AWS only |

## CNI Performance Benchmarking

Khi chon CNI, performance la yeu to quan trong. Methodology benchmark bao gom:

1. **Preparation** - Setup nodes, Kubernetes, CNI
2. **Idle Performance** - CPU/memory overhead sau khi cai CNI
3. **Pod-to-Pod TCP Bandwidth** - Single stream va multiple streams (8 threads)
4. **Pod-to-Pod UDP Bandwidth** - Single stream va multiple streams
5. **Pod-to-Service TCP/UDP Bandwidth** - Qua kube-proxy (iptables vs IPVS)

Cac cau hinh thuong duoc so sanh:
- **Calico + kube-proxy + iptables**: Setup pho bien nhat
- **Calico + kube-proxy + IPVS**: Tot hon khi co nhieu Services
- **Cilium + eBPF**: Thay the hoan toan kube-proxy, performance cao nhat

> Tham khao chi tiet: [Benchmark Kubernetes Network Plugins over 40Gbit/s (2024)](https://itnext.io/benchmark-results-of-kubernetes-network-plugins-cni-over-40gbit-s-network-2024-156f085a5e4e)

## Tong ket

| Cloud / Platform | CNI Approach | Dac diem |
|---|---|---|
| **GKE** (Routes-Based) | Custom static routes + cbr0 bridge | Legacy, Pod IP khong routable trong VPC |
| **GKE** (VPC-Native) | Alias IP ranges | Recommended, Pod IP natively routable |
| **AWS EKS** | VPC CNI + ENI + Secondary IPs | Pod nhan IP thuc tu VPC |
| **AWS EKS** (SG for Pods) | Trunk ENI + VLAN | Per-pod security groups |
| **Self-managed** (Flannel) | VXLAN overlay | Don gian, dev/test |
| **Self-managed** (Calico) | iptables / eBPF | Network policy, production |
| **Self-managed** (Cilium) | eBPF native | L7 policy, Hubble observability |

### Tiep theo

- [Container Networking from Scratch](/blog/container-networking-from-scratch) - Tu tay xay dung networking tu veth pair den CNI plugin
- [AWS VPC CNI Deep Dive](/blog/aws-vpc-cni-deep-dive) - ENIs, trunk networking, va policy routing tren AWS

### Tai nguyen tham khao

- [Kubernetes Networking Model](https://kubernetes.io/docs/concepts/cluster-administration/networking/)
- [CNI Specification](https://www.cni.dev/docs/spec/)
- [GKE VPC-Native Clusters](https://cloud.google.com/kubernetes-engine/docs/concepts/alias-ips)
- [AWS VPC CNI Plugin](https://github.com/aws/amazon-vpc-cni-k8s)
- [Cilium Documentation](https://docs.cilium.io/)
- [Benchmark: Kubernetes Network Plugins over 40Gbit/s](https://itnext.io/benchmark-results-of-kubernetes-network-plugins-cni-over-40gbit-s-network-2024-156f085a5e4e)
